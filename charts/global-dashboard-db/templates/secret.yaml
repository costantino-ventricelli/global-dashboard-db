apiVersion: v1
kind: Secret
metadata:
  name: {{ include "global-dashboard-db.fullname" . }}-secret
  labels:
    {{- include "global-dashboard-db.labels" . | nindent 4 }}
type: Opaque
stringData:
  jdbc-url: {{ .Values.database.url | quote }}
  jdbc-user: {{ .Values.database.username | quote }}
  jdbc-password: {{ .Values.database.password | quote }}
