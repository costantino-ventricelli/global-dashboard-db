apiVersion: v1
kind: Service
metadata:
  name: {{ include "global-dashboard-db.fullname" . }}
  labels:
    {{- include "global-dashboard-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "global-dashboard-db.selectorLabels" . | nindent 4 }}
