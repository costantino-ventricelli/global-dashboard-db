datasources:
  default:
    url: jdbc:tc:postgresql:14:///globaldashboard
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    dialect: POSTGRES

flyway:
  datasources:
    default:
      enabled: true
