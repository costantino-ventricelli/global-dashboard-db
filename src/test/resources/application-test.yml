datasources:
  default:
    url: jdbc:postgresql://localhost:5433/testdb
    driverClassName: org.postgresql.Driver
    username: testuser
    password: testpassword
    dialect: POSTGRES

micronaut:
  test-resources:
    enabled: false
  health:
    kafka:
      enabled: false